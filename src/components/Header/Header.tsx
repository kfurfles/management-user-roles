import React from 'react';
import { Styled } from './styles';

const Header: React.FC = () => {
  return (
  <Styled.Container>
      Procurar algo ...
  </Styled.Container>);
}

export default Header;